import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Signin | Claudio",
};

const SignInPage = () => {
  return (
    <div className="fixed w-full h-full left-0 bg-black/80 text-white flex items-center justify-center">
      SignInPage
    </div>
  );
};

export default SignInPage;
